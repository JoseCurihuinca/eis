# Prompt de ejecución para Codex — EIS-40C.1

Implementa estrictamente el Enterprise Implementation Specification ubicado en:

```text
Architecture/EIS/EIS-40C.1/
```

Lee primero:

```text
Architecture/EIS/EIS-40C.1/README.md
```

El `README.md` define el orden obligatorio de lectura y ejecución de los demás documentos.

Antes de modificar código, completa íntegramente `Repository Discovery` y `Repository Analysis`.

Si detectas una contradicción bloqueante entre la especificación y el repositorio, detente y entrega el análisis técnico antes de implementar.

Implementa únicamente los cambios permitidos por el EIS.

No modifiques scoring, `ResultJson`, contratos públicos, frontend, narrativa, Vendor Comparison ni lógica productiva de evaluación.

Mantén el Rule Engine deshabilitado o en modo shadow no autoritativo.

No avances a EIS-40C.2 ni a fases posteriores.

Al finalizar, entrega exclusivamente el reporte definido en:

```text
Architecture/EIS/EIS-40C.1/06-Report.md
```

No hagas commit.

No hagas push.

No hagas merge.
